[package]
name = "test1"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1"
rust_decimal = { version = "1", features = ["serde-with-str"] }
rust_decimal_macros = "1"
serde = { version = "1", features = ["derive"] }
